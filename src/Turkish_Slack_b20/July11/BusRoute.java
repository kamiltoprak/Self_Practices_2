package Turkish_Slack_b20.July11;

import java.util.Scanner;

public class BusRoute {
    public static void main(String[] args) {
        /*Scanner scan = new Scanner(System.in);
        System.out.print("Enter the first Bus Stop: ");
        char start1 = scan.next().charAt(0);
        System.out.print("Enter the last Bus Stop: ");
        char end1 = scan.next().charAt(0);
        String path = "A> right B> down C> left D> up :";
        String busStops = "[ABCD]";
        String removeExceptEnd = busStops.replace("" + end1, "");
​
        if (path.indexOf(start1) < path.indexOf(end1)) {
            path = path.substring(path.indexOf(start1) + 3, path.indexOf(end1) - 1);
            path = path.replaceAll(removeExceptEnd, "");
            path = path.replace(" " + end1 + ":", ": ");
            System.out.println(path + ": " + end1 + " found");
        } else if (path.indexOf(start1) > path.indexOf(end1)) {
            String path1 = path.substring(path.indexOf(start1) + 3, path.indexOf(":"));
            String path2 = path.substring(0, path.indexOf(end1));
            path = (path1 + path2);
            path = path.replaceAll(removeExceptEnd, "");
            path = path.trim();
            System.out.println(path + ": " + end1 + " found");
        } else {
            System.out.println(end1 + " found");
        }*/

    }
}
